

@using (Content.BeginScope(Content.Traverse.StartPage))
{
    var imgTag = string.Format("/upload/{0}", Content.Render.Image("Logotype").ToString().Split('/')[2].Split('"')[0]);
    var path = string.Format("/image.ashx?img={0}&w=100&h=75", imgTag);
    <img src="@imgTag" />
}